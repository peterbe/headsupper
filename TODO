TODO
----

* /about page

* run `npm run deploy` in travis and see that it worked

* ability to see past payloads

* Clean away all django-pipeline or jinja junk we don't need

* Add a note at the bottom of the instructions that says to expect an
  email from `Headsupper.io <oi@headsupper.io>` that might need to be
  manually approved from your spam folder.



DONE
----

* fix bug with toggling on the advanced fields

* Ability to delete a config

* Lazy load the handwriting font
