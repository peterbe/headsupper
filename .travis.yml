language: python
python:
  - "2.7"
addons:
  postgresql: "9.3"
before_script:
  - createdb headsupper
install:
  - pip install tox coveralls
env:
  - TOX_ENV=flake8
  - TOX_ENV=docs
  - TOX_ENV=tests
script:
  - tox -e $TOX_ENV
  - npm install
  - npm run deploy
after_success:
  - coveralls
